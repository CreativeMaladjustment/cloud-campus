# cloud-campus
Bite-sized AWS examples and cloud deployment guides for quick reference.

## Workflows

*   **Test S3 CloudFormation Deployment:** Tests deploying an AWS CloudFormation template that provisions an S3 bucket (with Glacier lifecycle rules and an ephemeral bucket) to LocalStack, ensuring syntax and basic functionality are correct without requiring a real AWS environment.
